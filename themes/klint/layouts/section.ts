// Seriph's `section` layout, unchanged. Re-exported rather than copied so upstream
// fixes arrive with a version bump instead of a manual merge.
export { default } from '@slidev/theme-seriph/layouts/section.vue'
