// Seriph's stylesheet, unchanged. The font families themselves come from
// `slidev.defaults.fonts` in package.json, which slidev turns into the
// --slidev-font-* variables these styles consume.
import '@slidev/theme-seriph/styles/index.ts'
